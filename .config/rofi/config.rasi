configuration {
    modes: [ combi ];
    combi-modes: [ drun, ssh, window ];
    show-icons: true;
    dpi: 192;

    timeout {
        action: "kb-cancel";
        delay:  0;
    }

    filebrowser {
        directories-first: true;
        sorting-method:    "name";
    }
}

@theme "arthur.rasi"

element {
    children: [ element-text, element-icon ];
}

element-icon {
    size: 1em;
}
